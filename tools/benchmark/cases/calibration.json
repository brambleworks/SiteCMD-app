[
  {
    "id": "credentialed-cors",
    "repository": "seeded-account-api",
    "kind": "repair",
    "category": "security",
    "rule": "cors-origin-reflection",
    "runtime": "node",
    "entry": "app/api/account/route.mjs",
    "prompt": "Review the account endpoint's cross-origin response policy and fix any unsafe behavior. Preserve the documented clients and response contract.",
    "requirements": "Only https://dashboard.example.com and https://admin.example.com may read credentialed cross-origin responses. Missing and untrusted origins must not receive Access-Control-Allow-Origin. Approved origins must receive credentials support. Preserve status 200 and the JSON account payload.",
    "files": {
      "package.json": "{\"private\":true,\"type\":\"module\",\"scripts\":{\"test\":\"node --test\"}}\n",
      "app/api/account/route.mjs": "export function GET(request) {\n  const headers = new Headers({ 'Content-Type': 'application/json' });\n  headers.set('Access-Control-Allow-Origin', request.headers.get('origin'));\n  headers.set('Access-Control-Allow-Credentials', 'true');\n  return new Response(JSON.stringify({ account: 'demo', plan: 'free' }), { headers });\n}\n"
    },
    "reference": {
      "app/api/account/route.mjs": "const allowedOrigins = ['https://dashboard.example.com', 'https://admin.example.com'];\n\nexport function GET(request) {\n  const headers = new Headers({ 'Content-Type': 'application/json', Vary: 'Origin' });\n  const origin = request.headers.get('origin');\n  if (allowedOrigins.includes(origin)) {\n    headers.set('Access-Control-Allow-Origin', origin);\n    headers.set('Access-Control-Allow-Credentials', 'true');\n  }\n  return new Response(JSON.stringify({ account: 'demo', plan: 'free' }), { headers });\n}\n"
    }
  },
  {
    "id": "redirect-destination",
    "repository": "seeded-account-api",
    "kind": "repair",
    "category": "security",
    "rule": "open-redirect",
    "runtime": "node",
    "entry": "app/api/continue/route.mjs",
    "prompt": "Review the continue endpoint's destination handling and fix any unsafe redirect behavior. Preserve supported local destinations and the response contract.",
    "requirements": "Only same-origin paths beginning with a single slash may be used as destinations. Preserve their query and fragment under standard URL normalization. Missing, absolute, protocol-relative, backslash-normalized external and non-HTTP scheme destinations must go to /dashboard. URL-ignored tabs, newlines and carriage returns must not bypass these restrictions, even when a normalized network-path destination has the same host. Responses remain redirects with status 302.",
    "files": {
      "package.json": "{\"private\":true,\"type\":\"module\",\"scripts\":{\"test\":\"node --test\"}}\n",
      "app/api/continue/route.mjs": "export function GET(request) {\n  const url = new URL(request.url);\n  const next = url.searchParams.get('next') || '/dashboard';\n  return Response.redirect(new URL(next, url.origin), 302);\n}\n"
    },
    "reference": {
      "app/api/continue/route.mjs": "export function GET(request) {\n  const url = new URL(request.url);\n  const next = url.searchParams.get('next') || '/dashboard';\n  let target = new URL('/dashboard', url.origin);\n  const normalized = next.replace(/[\\t\\n\\r]/g, '');\n  if (next.startsWith('/') && !/^\\/[/\\\\]/.test(normalized)) {\n    const candidate = new URL(next, url.origin);\n    if (candidate.origin === url.origin) target = candidate;\n  }\n  return Response.redirect(target, 302);\n}\n"
    }
  },
  {
    "id": "user-lookup-query",
    "repository": "seeded-document-service",
    "kind": "repair",
    "category": "data",
    "rule": "python-sql-injection",
    "runtime": "python",
    "entry": "app/api/users.py",
    "prompt": "Review the user lookup's handling of request-supplied names and fix any unsafe behavior. Preserve exact-match lookup and the result shape.",
    "requirements": "Treat the supplied name as data, including quotes and SQL punctuation. Return only the matching user's id and name, or an empty list. Never return other users or modify records. The connection and request objects are supplied by the application.",
    "files": {
      "app/api/users.py": "def lookup(connection, request):\n    cursor = connection.cursor()\n    cursor.execute(f\"SELECT id, name FROM users WHERE name = '{request.args['name']}'\")\n    return cursor.fetchall()\n"
    },
    "reference": {
      "app/api/users.py": "def lookup(connection, request):\n    cursor = connection.cursor()\n    cursor.execute(\"SELECT id, name FROM users WHERE name = ?\", (request.args['name'],))\n    return cursor.fetchall()\n"
    }
  },
  {
    "id": "document-download",
    "repository": "seeded-document-service",
    "kind": "repair",
    "category": "security",
    "rule": "python-path-traversal",
    "runtime": "python",
    "entry": "app/api/download.py",
    "prompt": "Review the document download's handling of request-supplied paths and fix any unsafe behavior. Preserve access to documents under the configured root.",
    "requirements": "Return UTF-8 document contents only for regular files contained in the configured root, including nested documents. Reject absolute paths, traversal outside the root, escaping symlinks and non-regular files with ValueError or PermissionError. Missing documents may raise FileNotFoundError. Paths use POSIX semantics, including literal backslashes and drive-like filenames; the configured root may be '/'. Filesystem entries are stable during a call. The root and request objects are supplied by the application.",
    "files": {
      "app/api/download.py": "import os\n\ndef download(root, request):\n    with open(os.path.join(root, request.args['path']), encoding='utf-8') as document:\n        return document.read()\n"
    },
    "reference": {
      "app/api/download.py": "from pathlib import Path\nimport stat\n\ndef download(root, request):\n    base = Path(root).resolve()\n    supplied = Path(request.args['path'])\n    if supplied.is_absolute():\n        raise ValueError('Absolute document path')\n    candidate = (base / supplied).resolve()\n    if not candidate.is_relative_to(base):\n        raise ValueError('Document is outside root')\n    if not stat.S_ISREG(candidate.stat().st_mode):\n        raise ValueError('Document is not a regular file')\n    with candidate.open(encoding='utf-8') as document:\n        return document.read()\n"
    }
  },
  {
    "id": "bound-user-lookup",
    "repository": "seeded-document-service",
    "kind": "negative_control",
    "category": "data",
    "rule": null,
    "runtime": "python",
    "entry": "app/api/users.py",
    "prompt": "Review the user lookup's handling of request-supplied names. Fix it only if there is a real defect; explain an intentional no-op if it is already safe. Preserve exact-match lookup and the result shape.",
    "requirements": "Treat the supplied name as data, including quotes and SQL punctuation. Return only the matching user's id and name, or an empty list. Never return other users or modify records. The connection and request objects are supplied by the application.",
    "files": {
      "app/api/users.py": "def lookup(connection, request):\n    cursor = connection.cursor()\n    cursor.execute(\"SELECT id, name FROM users WHERE name = ?\", (request.args['name'],))\n    return cursor.fetchall()\n"
    },
    "reference": {}
  }
]
